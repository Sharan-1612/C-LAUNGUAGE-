#include<stdio.h>
void main()
{
    int a,count=0;
    printf("Enter a number:");
    scanf("%d",&a);
    for(int i=2;i<a;i++)
    {
        if(a%i ==0)
            count=count+1;
    }
    if(count==0)
        printf("prime number");
    else
        printf("composite number");
}
